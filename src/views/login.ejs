<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"></script>

  <link rel="stylesheet" type="text/css" href="/cdn/css/login.css">
</head>

<body>
  <nav>
    <div id="loginlogout">
      <% if (isAuthenticated) { %>
        <button type="button" class="btn btn-success" id="logoutBtn">logout</button>
        <% } else { %>
          <button type="button" class="btn btn-success" id="loginBtn">login</button>
          <% } %>
    </div>
  </nav>
  <% if (locals.isAuthenticated) { %>
    <h1 class="text-4xl mb-4">Welcome <%= user.given_name%>

    </h1>
    <p>
      <%=JSON.stringify(user)%>
    </p>
    <% } else { %>
      <h1> Please login to continue
      </h1>
      <% } %>

        <!-- Only shown once the user selects login -->
        <div aria-live="polite" aria-atomic="true" style="position: relative; min-height: 600px;">
          <!-- Position it -->
          <div style="position: absolute; top: 0; right: 0;">
            <div class="toast" auto id="loginAlert">
              <div class="toast-header">
                <strong class="me-auto"><i class="bi bi-exclamation-circle"></i> Take note</strong>
                <!-- <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button> -->
              </div>
              <div class="toast-body">
                <div>
                  To login as a student use your student email account and to login as a lecturer please use your
                  university
                  lecturer account
                </div>
                <p></p>
                <!-- AuthZero -->
                <div>Proceed to login <a href="/login">Click here!</a></div>
              </div>
            </div>
          </div>
        </div>
        <script src="/cdn/scripts/login.js"></script>
</body>

</html>